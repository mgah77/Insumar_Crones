<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="ir_cron_weekly_stock_fix" model="ir.cron">
            <field name="name">Ajuste de Stock (Semanal)</field>
            <field name="model_id" ref="model_product_product"/>
            <field name="state">code</field>
            <field name="code">model.action_weekly_stock_adjustment()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">weeks</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
            <field name="active" eval="True"/>
            <!-- Nota: Puedes añadir <field name="nextcall">...</field> para forzar fecha inicio si deseas -->
        </record>
    </data>
</odoo>